/* ========================================================================= *
 * SelectByHeapsort
 * Implementation of the selection algorithm using heapsort
 * ========================================================================= */

#include <stddef.h>
#include "Select.h"

int select(int* array, size_t length, size_t k)
{
    // A compléter
    return array[0];
}
